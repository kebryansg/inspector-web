import { ActionsInspectionPipe } from './actions-inspection.pipe';

describe('ActionsInspectionPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionsInspectionPipe();
    expect(pipe).toBeTruthy();
  });
});
